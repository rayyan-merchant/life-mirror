def load_mock_image():
    """
    Returns a placeholder image bytes object.
    Can be extended to load from a local fixtures folder.
    """
    # Just return empty bytes for now in mock mode
    return b"MOCK_IMAGE_DATA"
